name: example
description: Example app for managed package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  managed:
    path: ../
  cupertino_icons: ^1.0.2
  oxidized: ^5.2.0
  device_calendar: ^4.2.0
  go_router: ^5.0.5
  flutter_portal: ^1.1.1
  lumberdash: ^3.0.0
  colorize_lumberdash: ^3.0.0
  mobx: ^2.1.0
  flutter_mobx: ^2.0.6+4
  permission_handler: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true